@model List<Repositories.Consorcio>

@{
    Layout = "~/Views/Shared/_LogueadoLayout.cshtml";
    ViewBag.Title = "Consorcios";
}

<section class="bg-white rounded">
    <div class="d-flex justify-content-between py-2 px-3">
        <h2>Listado Consorcios</h2>
        <p>
            @Html.ActionLink("Crear nuevo consorcio", "Add", null, new { @class = "btn btn-primary" })
        </p>
    </div>
    <table id="table" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.First().Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.First().Provincia.Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.First().Ciudad)
                </th>
                <th>
                    Unidades
                </th>
                <th>
                    Expensas
                </th>
                <th>
                    Gastos
                </th>
                <th>
                    Eliminar
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {

                <tr>
                    <td>
                        @Html.ActionLink(item.Nombre, "Update", new { id = item.IdConsorcio })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Provincia.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ciudad)
                    </td>
                    <td>
                        @Html.ActionLink("Ver Unidades", "Index", "Unidades", new { consorcioId = item.IdConsorcio }, null)
                    </td>
                    <td>
                        @Html.ActionLink("Ver Expensas", "Index", "Expensas", new { consorcioId = item.IdConsorcio }, null)
                    </td>
                    <td>
                        @Html.ActionLink("Ver Gastos", "Index", "Gastos", new { consorcioId = item.IdConsorcio }, null)
                    </td>
                    <td>
                        @Html.ActionLink(" ", "Delete", new { id = item.IdConsorcio }, new { @class = "fa fa-trash text-danger" })
                    </td>
                </tr>
            }

        </tbody>
    </table>
</section>