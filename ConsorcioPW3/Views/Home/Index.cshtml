@model Entities.Usuario

@{
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
    ViewBag.Title = "Home";
}

<div class="home">
    <h1 class="siblings-margin-30 text-center">Sistema para la Administración de Consorcios</h1>

    <section class="container w-50 bg-white px-5 py-4 rounded">
        <h3 class="mb-4 text-center">Ingresar</h3>
        <div class="tab-content" id="pills-tabContent">
            <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
                <form action="/Home/Login" method="post" novalidate>
                    @Html.AntiForgeryToken()
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label for="inputEmail-ingresar">Email</label>
                            @Html.EditorFor(Model => Model.Email, new { htmlAttributes = new { @class = "form-control", @placeholder = "Email" } })
                            @Html.ValidationMessageFor(Model => Model.Email, "", new { @class = "text-danger" })
                            <p class="text-danger m-0">@ViewBag.LoginError</p>
                        </div>
                        <div class="form-group col-md-12">
                            <label for="inputPassword-ingresar">Contraseña</label>
                            @Html.PasswordFor(Model => Model.Password, new { @class = "form-control", @placeholder = "Contraseña" } )
                            
                        </div>
                    </div>
                    <button type="submit" class="btn button-primary">Sign in</button>
                </form>
            </div>
        </div>
        <p class="mt-3">Todavia no te hiciste una cuenta? <a class="navigation-color" href="/Home/Register">Registrate!</a></p>
    </section>
</div>
